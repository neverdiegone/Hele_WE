<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <declare-styleable name="PassWordEditText">
        <attr name="useStrike" format="boolean"></attr>
    </declare-styleable>

    <style name="Base_ToolbarStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="android:background">@color/blue</item>
        <item name="android:theme">@style/Base_ToolbarStyle</item>
        <item name="titleTextAppearance">@color/black</item>
        <item name="subtitleTextAppearance">@color/black</item>
    </style>
</resources>
